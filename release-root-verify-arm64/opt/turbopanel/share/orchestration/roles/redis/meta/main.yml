---
galaxy_info:
  author: turbopanel
  description: >-
    Build and install a pinned Redis binary into the shared runtimes directory;
    dedicated redis system user; Unix socket only (port 0, no TCP listener).
    Requires 64-bit Linux (aarch64 or x86_64).
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all

dependencies: []
