{
  "private": true,
  "scripts": {
    "stage": "mkdir -p public && cp ../../scripts/run.sh public/run.sh",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.109.0"
  }
}
