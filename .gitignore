node_modules/
orchestration/runtime/
# Ansible Galaxy deps — installed during bootstrap (see orchestration/requirements.yml).
orchestration/roles/geerlingguy.docker/
orchestration/collections/

# env and secret-bearing dotenv variants
.env
.env.*
.env-*
.envrc
!.env.example
.dev.vars
.dev.vars*
!.dev.vars.example

# keys, certs, tokens, and passwords
*.pem
*.key
*.p12
*.jks
*.mobileprovision
*.token
*.secret
*.pass
server.id
server-key.json
server-key-id
license.id
license.token
cloudflared/tunnels/*.token
certs/

dist/*
turbopaneld
logs/
state/
.local/
.cache/
.config/

# co-located dev host stamp (normalize-dev-checkout.sh)
.turbopanel-checkout-stamp
