---
# Ansible Galaxy roles installed into orchestration/roles/ during bootstrap.
# Pinned versions keep installs reproducible across agent nodes.
roles:
  - name: geerlingguy.docker
    version: "8.0.0"

collections:
  - name: ansible.posix
    version: ">=1.5.0"
