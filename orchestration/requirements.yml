---
# Ansible Galaxy roles and collections installed during bootstrap.
# Exact version pins keep installs reproducible across managed server daemons.
roles:
  - name: geerlingguy.docker
    version: "8.0.0"

collections:
  - name: ansible.posix
    version: "2.2.1"
