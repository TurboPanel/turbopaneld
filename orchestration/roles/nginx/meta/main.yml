---
galaxy_info:
  author: turbopanel
  description: >-
    Vendor a pinned nginx.org binary package into the shared runtimes
    directory (never a distro apt install) for site static hosting.
    Requires 64-bit Linux (aarch64 or x86_64). Identity (tpnginx) is provisioned
    separately via the web-service-user role.
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all

dependencies: []
