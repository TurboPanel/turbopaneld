---
galaxy_info:
  author: turbopanel
  description: >-
    Install and manage the systemd units for the self-hosted instance, Caddy
    reverse proxy, and (in dev) the Expo UI server, all running as the instance
    user in the turbopanel group.
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all
    - name: Raspbian
      versions:
        - all

dependencies: []
