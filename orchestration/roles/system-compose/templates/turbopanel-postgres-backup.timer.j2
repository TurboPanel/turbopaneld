# Managed by TurboPanel (system-compose role) — do not edit by hand.
# Nightly control-plane Postgres backup — see postgres-backup.sh and
# website/docs/deployment/security.mdx "Backup and disaster recovery".
[Unit]
Description=TurboPanel control-plane Postgres backup (nightly)
After={{ system_compose_service_name }}.service

[Timer]
OnCalendar={{ postgres_backup_oncalendar }}
Persistent=true
Unit=turbopanel-postgres-backup.service

[Install]
WantedBy=timers.target
