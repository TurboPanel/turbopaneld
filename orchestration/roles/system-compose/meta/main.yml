---
galaxy_info:
  role_name: system_compose
  author: turbopanel
  license: AGPL-3.0
  description: >
    Run postgres/rabbitmq as a single Docker Compose project
    (turbopanel-system) instead of independently `docker run`
    containers/units. Reads the secrets already generated by the (trimmed)
    postgres/rabbitmq roles and renders whichever service(s) have
    generated config, so it works whether both, or only one, ran in
    this play.
  min_ansible_version: "2.20"
  platforms:
    - name: Debian
      versions:
        - trixie

dependencies:
  - role: docker
