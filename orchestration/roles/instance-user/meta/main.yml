---
galaxy_info:
  author: turbopanel
  description: >-
    Create the low-privilege `instance` user (UID 9998) that runs the
    self-hosted TurboPanel instance. Member of the turbopanel group with no
    group of its own.
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all
    - name: Raspbian
      versions:
        - all

dependencies: []
