---
galaxy_info:
  author: turbopanel
  description: Install and enable the turbopaneld systemd unit.
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all

dependencies:
  - role: daemon-logs
