---
galaxy_info:
  author: turbopanel
  description: >-
    FHS config, systemd unit, and state directories for the unprivileged
    per-site Caddy that serves tenant sites on loopback high ports. The binary
    comes from the sibling `caddy` role; identity (tpcaddysite) from
    web-service-user. Distinct from the edge Caddy (public :80/:443) and the
    control-plane Caddy (tpcaddy).
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all

dependencies: []
