# Placeholder so `import .../sites/*.conf` always matches at least one file.
# Caddy treats a zero-match import as an error, which would fail every
# `caddy validate` on a host that has no sites yet. Do not delete.
