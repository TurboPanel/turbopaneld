---
- name: Install agent prerequisites
  ansible.builtin.apt:
    name:
      - ca-certificates
      - curl
      - git
      - screen
      - tar
      - unzip
    state: present
    update_cache: true
