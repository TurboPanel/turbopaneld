---
- name: Install agent prerequisites
  ansible.builtin.apt:
    name:
      - acl
      - build-essential
      - ca-certificates
      - curl
      - git
      - gnupg
      - iptables
      - libssl-dev
      - pamtester
      - pkg-config
      - python3-debian
      - tar
      - unzip
      - xz-utils
    state: present
    update_cache: true
