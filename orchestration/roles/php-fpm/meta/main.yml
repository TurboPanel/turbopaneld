---
galaxy_info:
  author: turbopanel
  description: >-
    Build a pinned PHP + php-fpm release from official source into the shared
    runtimes directory (never a distro apt install) for traditional-web Apache
    PHP via proxy_fcgi. Requires 64-bit Linux (aarch64 or x86_64). Identity
    (tpapache) is provisioned separately via the web-service-user role.
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - all

dependencies: []
