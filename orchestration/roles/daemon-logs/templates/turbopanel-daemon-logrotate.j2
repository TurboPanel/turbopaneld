# Managed by TurboPanel daemon orchestration (daemon-logs role).
{{ daemon_log_dir }}/*.log {
    daily
    rotate {{ daemon_logrotate_rotate }}
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
