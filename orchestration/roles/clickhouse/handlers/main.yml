---
# Container config-reload is handled by the explicit "Restart analytics
# container when overlay config changed" task (docker restart) in
# ../system-compose/tasks/main.yml, so this role has no handlers.
