- name: TurboPanel RabbitMQ setup
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  vars:
    turbopanel_config_dir: /opt/turbopanel/platform/config
  roles:
    - role: docker
    - role: rabbitmq
