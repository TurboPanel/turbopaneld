- name: TurboPanel queue setup
  hosts: localhost
  connection: local
  become: true
  gather_facts: true
  vars:
    # Standalone managed broker setup (daemon-only hosts) — FHS config layout.
    turbopanel_config_dir: /etc/turbopanel
  roles:
    - role: docker
    - role: rabbitmq
    - role: system-compose
