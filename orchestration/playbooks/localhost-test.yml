- name: TurboPanel orchestration smoke test
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Ping localhost
      ansible.builtin.ping:

    - name: Confirm runtime
      ansible.builtin.debug:
        msg: "Orchestration runtime is operational"
