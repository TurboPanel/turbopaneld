[defaults]
# IDE ansible-lint runs with cwd at the daemon repo root; playbooks and roles
# live under orchestration/.
roles_path = orchestration/roles
collections_path = vendor/ansible/galaxy-collections:/opt/turbopanel/vendor/ansible/galaxy-collections:/usr/share/ansible/collections
